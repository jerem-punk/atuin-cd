# atuin-cd

A zsh function to navigate easily to previous working directories.

## how it works

It get the working directories stored in the atuin sqlite database, pipe them to fzf and cd into the selected one.

Simple but addictive.

